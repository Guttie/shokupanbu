<main>
  <h2 class="title">TOASTS</h2>
  <div class="container">
    <div class="row">
      <div class="col-12">
        <% if @toasts.exists? %>
          <div class="d-flex flex-wrap">
            <% @toasts.each do |toast| %>
              <div id="slider_<%= toast.id %>" class="col col-4 mb-5">
                <% toast.toast_images.each do |image| %>
                  <%= link_to toast_path(toast.id) do %>
                    <%= attachment_image_tag image, :image, format: 'jpeg', size: "350x350", class: "d-block mx-auto content" %>
                  <% end %>
                <% end %>
              </div>
            <% end %>
          </div>
        <% else %>
          <p>投稿はありません。</p>
        <% end %>
      </div>
    </div>
  </div>
</main>

<script>
  <% @toasts.each do |toast| %>
    $(function() {
      $('#slider_<%= toast.id %>').slick({
        speed: 900,
        arrows: false,
        autoplay: true,
        autoplaySpeed: 6000,
      });
    });
  <% end %>
</script>