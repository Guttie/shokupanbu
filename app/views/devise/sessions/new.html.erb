<main>
  <h2 class="title">SIGN IN</h2>
  <div class="container">
    <div class="row">
      <div class="col-lg-6 mx-auto">
        <h3>サインイン</h3>
        <p class="text-danger mb-2">※印は必須項目です。</p>
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>
          <div class="form-group">
            <%= f.label :email, "メールアドレス" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "(例)sample@sample.com", class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :password, "パスワード" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
          </div>
          <% if devise_mapping.rememberable? %>
            <div class="form-group">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me, "ログイン状態を維持" %>
            </div>
          <% end %>
          <div class="my-5">
            <%= f.submit "Sign in", class: "btn btn-outline-success mx-auto d-block" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</main>
