<main>
  <h2 class="title">PASSWORD RESET</h2>
  <div class="container">
    <div class="row">
      <div class="col-md-6 mx-auto">
        <h3>パスワードリセット</h3>
        <p>
          下記のフォームに登録済みのメールアドレスを記入してください。<br/>
          ご登録のメールアドレスにパスワードの再設定画面のURLを記載したメールをお送りします。
        </p>
        <%= form_with model: @user, url: new_user_password_path do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          <div class="form-group">
            <%= f.label :email, "メールアドレス" %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "(例)sample@sample.com", class: "form-control" %>
          </div>
          <div class="my-5">
            <%= f.submit "送信する", class: "btn btn-outline-success mx-auto d-block" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</main>